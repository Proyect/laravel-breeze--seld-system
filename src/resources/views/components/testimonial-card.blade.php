<div class="bg-white rounded-xl shadow p-6 flex flex-col items-center text-center">
    <img src="{{ $avatar }}" alt="{{ $name }}" class="w-16 h-16 rounded-full mb-4 border-2 border-blue-500">
    <h4 class="font-bold text-lg text-blue-700">{{ $name }}</h4>
    <span class="text-sm text-gray-500 mb-2">{{ $role }}</span>
    <p class="text-gray-700 italic">"{{ $testimonial }}"</p>
</div> 